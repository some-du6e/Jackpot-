50 chips = $6.00 dollars = 1 hour  (UPDATED Mar 20, 2026 — online items now $6/h after Jackpot Las Vegas cancellation)

NOTE: `existingHoursForItem` is intentionally sourced from the toolbar stat stored in `localStorage` under `jackpot_plus_stats.hours`. This makes goal progress reflect the toolbar's total hours value rather than per-card sums. Update later if you want per-project matching instead.


## HTML dumps
Whenever you have access to the browser tool, please update the agentsstuff folder with updated dumps please
Most updated htmls are in the agentstuff folder

## New features
When adding a new feature add it to the readme so i dont forget